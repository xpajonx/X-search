---
name: x-search
description: Search X (Twitter) using browser-cookie authentication. Keyword search, user search, thread fetch — replaces the xAI x_search tool with a local CLI.
---

# x-search

This project uses the global x-search skill. See `~/.config/opencode/skills/x-search/SKILL.md` for the full workflow.

The global skill is the canonical source. This file exists only so the project registers the skill name.
